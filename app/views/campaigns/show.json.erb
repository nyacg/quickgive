{
  "campaignerName": "<%= @campaign.user.first_name %> <%= @campaign.user.last_name %>",
  "initialTarget": <%= @campaign.target %>,
  "stretchTarget": <%= @campaign.strgoalamm %>,
  "currentTotal": <%= @campaign.total_raised %>,
  "donors": [
    {"name": "Initial donations", "date": "<%= time_ago_in_words @campaign.created_at %>", "amount": <%= @campaign.initial %>, "via": "Initial"},
    <% @campaign.payments.each do |payment| %>
    {"name": "<%= payment.user.first_name %> <%= payment.user.last_name %>", "date": "<%= time_ago_in_words payment.time %>", "amount": <%= payment.amount %>, "via": "<%= payment.via %>"}<%= "," unless payment == @campaign.payments.last %>
    <% end %>
  ]
}
